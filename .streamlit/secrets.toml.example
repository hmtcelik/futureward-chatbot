# Streamlit Cloud secrets — paste this into the Cloud dashboard's
# Secrets editor at https://share.streamlit.io. Do NOT commit the real
# secrets.toml; .gitignore already excludes it.
GEMINI_API_KEY = "paste-your-api-key-here"
